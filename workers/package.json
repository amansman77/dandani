{
  "name": "dandani-workers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.20.4"
  }
}
